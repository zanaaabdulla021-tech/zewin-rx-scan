/// Edit before you build: set this to your deployed backend's base URL
/// (see /backend/README.md), no trailing slash. The same backend handles
/// login, branches, users, prescriptions, and the OCR proxy.
class AppConfig {
  static const String backendUrl = '';
}
